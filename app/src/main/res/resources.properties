# Declares the locale of the default (unqualified) values/ resources so AGP can generate
# locales_config.xml for Android 13+ Per-App Language Preferences (generateLocaleConfig=true).
# The base strings are authored in English; values-uk/ supplies the Ukrainian translation.
unqualifiedResLocale=en-US
